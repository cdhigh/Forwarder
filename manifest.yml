---
applications:
  .:
    name: forwarder
    framework:
      name: wsgi
      info:
        mem: 64M
        description: Python WSGI Application
        exec: 
    infra: ap-aws
    url: ${name}.${target-base}
    mem: 64M
    instances: 8
